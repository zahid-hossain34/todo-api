/**
 * @typedef {{ id: number, title: string, description: string, completed: boolean, createdAt: string, updatedAt?: string }} Todo
 */

module.exports = {}; // Empty for now; could add schema validation (e.g., Joi) later